<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Listado de Items</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.4.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Google Fonts - Roboto -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <!-- Custom CSS -->
  <link href="/css/style.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>
<body>
  <div class="container py-4">

    <!-- Header con bandera de colores -->
    <div class="header mb-4 position-relative">
      <div class="color-stripes">
        <div class="stripe blue"></div>
        <div class="stripe white"></div>
        <div class="stripe red"></div>
      </div>
      <h1 class="display-4 fw-bold">Mis Items</h1>
      <p class="lead">Administra tus elementos desde aquí</p>
    </div>

    <!-- Formulario para agregar -->
    <div class="row justify-content-center mb-5">
      <div class="col-md-8">
        <form action="/items" method="POST" class="input-group shadow">
          <input
            type="text"
            name="name"
            class="form-control form-control-lg"
            placeholder="Nuevo ítem..."
            required
            aria-label="Nuevo ítem">
          <button class="btn btn-success btn-lg" type="submit">
            <i class="bi bi-plus-lg me-2"></i>Agregar
          </button>
        </form>
      </div>
    </div>

    <!-- Lista de items -->
    <% if (items.length === 0) { %>
      <div class="row justify-content-center">
        <div class="col-md-8">
          <div class="alert alert-info text-center py-3">
            <i class="bi bi-info-circle-fill me-2"></i>
            No hay items aún. ¡Agrega el primero arriba!
          </div>
        </div>
      </div>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-2 g-4">
        <% items.forEach(i => { %>
          <div class="col">
            <div class="card card-item shadow-sm">
              <div class="card-body d-flex justify-content-between align-items-center">
                <span class="fs-5 fw-medium text-primary"><%= i.name %></span>
                <div class="d-flex gap-2">
                  <!-- Formulario inline edición -->
                  <form action="/items/<%= i.id %>" method="POST" class="d-flex">
                    <input
                      name="name"
                      value="<%= i.name %>"
                      class="form-control form-control-sm me-2"
                      style="width:120px;"
                      required>
                    <button class="btn btn-sm btn-primary" type="submit" title="Guardar">
                      <i class="bi bi-save"></i>
                    </button>
                  </form>

                  <!-- Eliminar -->
                  <form action="/items/<%= i.id %>/delete" method="POST">
                    <button class="btn btn-sm btn-danger" type="submit" title="Eliminar">
                      <i class="bi bi-trash"></i>
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.4.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <!-- Estilos adicionales para las banderas -->
  <style>
    .color-stripes {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 8px;
      display: flex;
    }
    .stripe {
      height: 100%;
      flex: 1;
    }
    .stripe.blue { background-color: #1a73e8; }
    .stripe.white { background-color: #ffffff; }
    .stripe.red { background-color: #d32f2f; }
  </style>
</body>
</html>